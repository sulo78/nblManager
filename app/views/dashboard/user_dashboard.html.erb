<% @title = "User dashboard" %>

<h2>Notebooks auswählen</h2>

<% unless @notebook.is_lent? %>
  <% @notebooks.each do |nb| %>
    <table>
      <tr>
        <th>Notebookname</th>
        <th>Hersteller</th>
        <th>Modell</th>
        <th>Service-Tag</th>
        <th>Pool</th>
        <th>Standort</th>
        <th></th>
        <th></th>
        <th></th>	
      </tr>
    
      <tr>
        <td><%= nb.nb_name %></td>
        <td><%= nb.manufactor %></td>
        <td><%= nb.nb_type %></td>
        <td><%= nb.serial %></td>
        <td><%= nb.pool.try(:pool_name) %></td>
        <td><%= nb.location.try(:loc_acronym) %></td>
        <td>( <%= link_to "details", notebook_path(nb) %></td>
      </tr>
    <% end %>  
  </table>
<% end %>
